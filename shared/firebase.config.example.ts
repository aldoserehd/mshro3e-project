/**
 * Firebase client config — PLACEHOLDER.
 *
 * Setup steps:
 *   1. Create Firebase project at https://console.firebase.google.com
 *   2. Add a Web app → copy the firebaseConfig object
 *   3. Copy this file to `shared/firebase.config.ts` (gitignored)
 *   4. Paste your real values below
 *
 * This file is committed; the real config is NOT.
 */

export const firebaseConfig = {
  apiKey: 'REPLACE_ME',
  authDomain: 'REPLACE_ME.firebaseapp.com',
  projectId: 'REPLACE_ME',
  storageBucket: 'REPLACE_ME.appspot.com',
  messagingSenderId: 'REPLACE_ME',
  appId: 'REPLACE_ME',
};

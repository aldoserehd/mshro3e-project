/**
 * App-wide configuration constants.
 */

/**
 * Public vendor marketing / sign-up website.
 *
 * Mshro3e never handles vendor sign-up or payment inside the customer app — the
 * "List your business" CTA opens this URL in the browser. Vendors choose a plan
 * (1 month or 3 months) and complete onboarding on the website at the `/join`
 * route.
 *
 * PLACEHOLDER: replace with the real public vendor site before release.
 */
export const VENDOR_SITE_URL = 'https://mshro3e.kw/join';
